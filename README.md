# Password Generator (Kotlin)

A simple command-line password generator written in Kotlin.

## Features
- Generate random passwords
- Customizable length
- Uses letters, numbers, and symbols

## Requirements
- JDK 8+
- Gradle

## Run

```bash
./gradlew run


Note: This project is mostly just an experiment however it is functional.
